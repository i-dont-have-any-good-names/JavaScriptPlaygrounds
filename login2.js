// work in progress

function a(i, i2) {
    var user = prompt("u");
    var password = prompt("p");
    var userV = false;
    var passV = false;
    if(user === i) {
        userV = true;
    if(password === i2) {
        passV = true;
    } else {
        passV = false;
        console.log("enter a vaid password");
    }
    login = userV, passV
    if(login === true, true) {
        console.log("logged in")
    } else {
        console.log("something went wrong")
    }
    } else {
        userV = false;
        console.log("eneter a vaid username");
    }
}

a("car", "a")

